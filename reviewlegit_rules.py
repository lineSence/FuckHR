"""Словари и веса проверки легитимности отзыва. Только данные.

Отделено от логики по [CORE-024] и [CORE-025]. Речь здесь не о правдивости
отзыва (это fake_reviews) и не о том, как он написан (aitext), а о более
раннем вопросе: это вообще отзыв сотрудника или кусок сайта, реклама и отзыв
клиента о товаре.

Ошибка в сторону «выбросили лишнее» дороже: выброшенный отзыв не оставляет
следа в досье. Поэтому признаков «за» больше, а отбрасывают текст только
жёсткие причины или заметный перевес признаков «против».
"""

from __future__ import annotations

import re

# Жёсткие причины: одна такая причина выбрасывает текст независимо от веса.
HARD = ("customer", "boilerplate", "employer_reply", "navigation")

# --- признаки «это отзыв сотрудника» ---
PLUS: dict[str, tuple[float, str]] = {
    "work_words": (2.0, "говорит о работе в компании"),
    "first_person": (1.5, "личный опыт от первого лица"),
    "pros_cons": (1.5, "разделён на плюсы и минусы"),
    "has_rating": (0.5, "есть оценка"),
    "has_date": (0.5, "есть дата"),
}

# --- признаки «это не отзыв» ---
MINUS: dict[str, tuple[float, str]] = {
    "customer": (5.0, "отзыв клиента о товаре или услуге, не о работе"),
    "boilerplate": (5.0, "строка встречается на страницах других компаний"),
    "employer_reply": (5.0, "это ответ работодателя, а не отзыв"),
    "navigation": (5.0, "меню, хлебные крошки или перечисление ссылок"),
    "reader_address": (1.5, "обращение к читателю, как в рекламе"),
    "promo": (1.5, "телефон, цена или ссылка в тексте"),
    "no_work_context": (2.0, "о работе в компании ничего не сказано"),
}

ACCEPT_AT = 1.0  # сумма «за» минус «против»; ниже — текст не отзыв

# Строка считается шаблоном площадки, если встретилась у стольких компаний.
BOILERPLATE_COMPANIES = 3

# Совпадение названия компании со страницей (rapidfuzz partial_ratio, 0..100).
COMPANY_MATCH = 80
COMPANY_MIN_CHARS = 4  # короткие названия сравнивать бессмысленно

# Список пополнен по живым страницам dreamjob и «правды сотрудников»: без
# «руководство», «директор» и «компания» настоящие отзывы вылетали как
# «о работе ни слова».
WORK_WORDS = (
    "работал", "работаю", "работала", "работы", "работе", "проработал",
    "устроил", "уволил", "уволен", "ушёл из компании", "ушел из компании",
    "коллектив", "коллег", "начальник", "руководител", "руководств",
    "директор", "компани", "зарплат", "зп ", "з/п", "оклад", "преми", "переработ",
    "испытательн", "собеседован", "трудоустрой", "отпуск", "больничн",
    "текучк", "смена", "сотрудник", "график", "должност", "обязанност",
    "оформлен", "команд", "офис",
)

FIRST_PERSON = (
    "я ", "мне ", "меня ", "мой ", "моя ", "нам ", "нас ", "у меня",
    "пришёл", "пришел", "ушёл", "ушел", "устроился", "устроилась",
)

# Клиентская лексика: отзыв о товаре или услуге, а не о работодателе.
CUSTOMER_WORDS = (
    "доставк", "заказ", "покупк", "купил", "купила", "товар", "магазин",
    "продавец", "возврат денег", "гарантийн", "курьер", "чек", "кассир",
    "обслуживание клиентов", "качество услуг", "ремонт квартиры",
    "записался на приём", "записалась на приём",
)

READER_WORDS = (
    "оставьте отзыв", "читайте также", "узнайте больше", "подпишитесь",
    "свяжитесь с нами", "оставить заявку", "перейти на сайт", "смотрите также",
    "наша компания предлагает", "мы предлагаем вам",
)

REPLY_WORDS = (
    "ответ компании", "ответ работодателя", "ответ представителя",
    "спасибо за ваш отзыв", "благодарим за отзыв", "hr-служба",
)

PHONE_RE = re.compile(r"(?:\+7|\b8)[\s(\-]?\d{3}[\s)\-]?\d{3}[\s\-]?\d{2}[\s\-]?\d{2}")
PRICE_RE = re.compile(r"\d[\d\s]{2,}\s?(?:₽|руб\.?|рублей)\s?(?:за|/)\s?(?:шт|кг|м2|час)")
URL_RE = re.compile(r"https?://|www\.", re.IGNORECASE)

# Навигация: много разделителей и почти нет предложений.
SEPARATORS = ("|", "›", "»", "•", " / ")
MIN_SENTENCE_MARKS = 1
MAX_SEPARATORS = 2

# Нормализация названия компании не дублируется: она живёт в company_key
# [DOC-002].

__all__ = tuple(name for name in dir() if name.isupper())

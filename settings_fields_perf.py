"""Каталог настроек: темп и параллелизм сетевых этапов.

Отдельный файл по той же причине, что settings_fields_gates.py: основной
каталог упёрся в 25 КБ [CORE-024]. Группа связная — «сколько потоков и с каким
темпом мы ходим наружу» (docs/performance.md).
"""

from __future__ import annotations

from settings_fields import FLOAT, INT, Field

GROUP_PERF = "Темп и потоки"
GROUP_PERF_HINT = "сколько запросов идёт одновременно; темп к источнику держит общий ограничитель"

PERF_FIELDS: tuple[Field, ...] = (
    Field(
        "HH_DETAIL_WORKERS",
        "Потоков на карточки вакансий",
        GROUP_PERF,
        INT,
        "3",
        "Сколько страниц вакансий hh.ru качается одновременно. Средний темп "
        "запросов не меняется — его держит общий ограничитель (HH_PAUSE), "
        "потоки лишь не дают простаивать паузе, пока ждём ответ. 1 — как было "
        "раньше, потолок 4.",
    ),
    Field(
        "SEARCH_PAUSE",
        "Пауза между запросами к поиску, с",
        GROUP_PERF,
        FLOAT,
        "0.5",
        "Минимальный интервал между обращениями к своему SearXNG или платному "
        "поиску. Держится общим ограничителем на все потоки, поэтому рост "
        "SEARCH_WORKERS и RESEARCH_WORKERS не ускоряет обращения к источнику. "
        "0 — без ограничения.",
    ),
    Field(
        "CONTACT_WORKERS",
        "Потоков на поиск контактов",
        GROUP_PERF,
        INT,
        "4",
        "Сколько компаний одновременно изучается на этапе контактов. Внутри "
        "компании запросы и так шли пулом, а сами компании стояли в очереди. "
        "Ограничение сверху — потолок запросов к поиску (SEARCH_MAX_CALLS).",
    ),
)

__all__ = ("GROUP_PERF", "GROUP_PERF_HINT", "PERF_FIELDS")

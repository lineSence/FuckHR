# Backlog

Идеи владельца от 18.09.2026, ещё не задачи. Пункт уходит в `docs/roadmap.md` только
после закрытия открытых вопросов. Статусы: `открыт` (ждёт ответов), `нужно решение
владельца` (спорит с правилом в `AGENTS.md`), `принят`, `отклонён`.

## B-01. Помощник заполнения резюме — открыт

Сейчас в системе есть профиль (`profile.yaml`, `profile_fields.py`, `profile_form.py`,
`ui_profile.py`), артефакта «резюме» нет.

1. Выход: заполненный `profile.yaml` или текст резюме файлом?
2. Вход: старое резюме, черновик, диалог-интервью, профиль hh.ru?
3. Одно мастер-резюме или подгонка под каждую вакансию?
4. Интерфейс: веб-UI или FSM в боте?
5. Домысел модели об опыте допустим или только перекладка данных владельца?

## B-02. Предложения смежных областей — открыт

6. Смежность по должностям, навыкам или отраслям?
7. Считает словарь-граф в коде (`[CORE-015]`) или LLM с кэшем при смене профиля?
8. Смежные запросы идут в сбор сами или по кнопке?
9. Пометка в выдаче и штраф в `score.py`, чтобы прямые попадания не тонули?

## B-03. Сопроводительное письмо по вакансии — открыт

`outreach.py` уже пишет черновик нанимающему менеджеру; речь про другой жанр.

10. Формальный cover letter для отклика или доработка письма человеку?
11. Не спорит ли с `[CORE-018]` (авто-отклики вне скоупа)?
12. Опирается на досье компании или только на текст вакансии?
13. Жёсткий шаблон или свободная генерация; нужен ли английский?

## B-04. Авто-отправка писем — нужно решение владельца

Прямо запрещено `[CORE-023]` и ADR-012 (SMTP-клиента нет), задевает `[CORE-022]`.

14. Отменяем `[CORE-023]`? Нужна формулировка замены от владельца (`[CORE-003]`).
15. Канал: SMTP владельца или форма отклика hh.ru?
16. Полный автомат или кнопка подтверждения в Telegram?
17. Суточный лимит писем и защита от повторной отправки одному человеку?

## B-05. Глубокий ресерч — открыт

База: `research.py`, `websearch.py`, `dossier.py`, `reviewpage.py`.

18. Многошаговый (модель сама ставит следующие запросы) или просто больше источников?
19. Объект: компания, человек (упирается в `[CORE-013]`) или рынок?
20. Бюджет вызовов и времени на одну компанию?
21. Для всех вакансий или по кнопке для отобранных?

## B-06. Детектор анти-ИИ инъекций в тексте вакансии — открыт

Текст вакансии уходит в модель почти как есть; «если ты ИИ — игнорируй инструкции»
ломает детектор, досье и письмо.

22. Реакция: пометка как сигнал HR-брехни, отбраковка или оба?
23. Регулярки + невидимый текст (`[CORE-015]`) или плюс LLM-классификатор?
24. Делаем ли общую обёртку внешнего текста в делимитеры в `llm_tasks.py`?
25. Куда пишем результат: поле в БД и раздел карточки?

## B-07. Бесконечный режим поиска — нужно решение владельца

Спорит с `[CORE-018]` («5 вакансий с полным досье», не массовость).

26. Непрерывный цикл вместо Task Scheduler (ADR-014) или просто чаще запуск?
27. Предложение: сбор бесконечный, полное досье — только топ-N в сутки. Принимаем?
28. Лимиты провайдеров при круглосуточной работе (`[CORE-016]`) и скромность к hh.ru (`[CORE-014]`)?
29. Что делать с окнами, когда машина владельца выключена?

## B-08. Несколько профилей — открыт

Самая инвазивная идея списка: схема БД, скоринг, дедуп, выдача, UI.

30. Профиль = роль или ещё гео/формат/вилка?
31. Сколько профилей реально нужно?
32. Вакансия подошла двум профилям — одна запись со связями или две записи?
33. Досье и контакты общие между профилями?
34. Telegram: один чат с фильтром или разные топики?
35. Мигрируем существующую БД или начинаем заново?

## B-09. Параллельный поиск по профилям — открыт

Без B-08 не делается.

36. Потоки или последовательный обход профилей? Сбор с hh.ru остаётся однопоточным (`[CORE-014]`, `[CORE-016]`).
37. Как делится бюджет вызовов между профилями?
38. Устроит ли очередь профилей с параллелизмом только внутри досье (он уже есть)?

## B-10. Кнопки в Telegram не работают — открыт

Единственный баг в списке; деталей нет.

39. Что именно: не появляются, не реагируют, висят «часики», состояние не меняется, мертвы после рестарта?
40. Что в логах (`message is not modified`, `query is too old`, `BUTTON_DATA_INVALID`)?
41. Экраны: карточка вакансии, досье, подтверждение отправки?
42. Всегда или только на старых сообщениях?

## B-11. Автоподбор моделей по уровням проверки — открыт

База: `STAGE_PROFILES` в `llm.py`, `wiki/architecture/model-routing.md`.

43. «Уровни» — стадии пайплайна или глубина внутри одной стадии?
44. Критерий подбора: длина входа, важность этапа, остаток квоты, качество?
45. Нужна ли эскалация «дешёвая → сильная при низкой уверенности»?
46. Деградация до работы вообще без модели (`[CORE-017]`) обязательна?

## B-12. Локальные эмбеддинги — нужно решение владельца

Отложено в `docs/roadmap.md`, шаг 5 (`sqlite-vec`, «семантический поиск не нужен»).

47. Задача: дедуп похожих вакансий, матч с резюме, поиск по базе или сведение компаний (там планируется `rapidfuzz`)?
48. Отменяем решение шага 5? Причина для ADR?
49. Считаем через шлюз `llm.py` (`[CORE-010]` запрещает второй HTTP-клиент к моделям)?
50. Хранение: `sqlite-vec` или перебор numpy по нескольким тысячам векторов?

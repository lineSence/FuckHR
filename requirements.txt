# Шаг 1: только то, без чего тонкий срез не работает.
# LangGraph, instructor, sqlite-vec, playwright и прочее добавляется на шаге 2+.
httpx>=0.27
pydantic>=2.7
PyYAML>=6.0
rapidfuzz>=3.9
aiogram>=3.7
python-dotenv>=1.0
# Проверка MX-записи домена при угадывании email (contacts.py, импорт ленивый).
dnspython>=2.6
# HTTP/2 к hh.ru (hh_html.py). Без этого пакета клиент молча остаётся на HTTP/1.1.
h2>=4.1

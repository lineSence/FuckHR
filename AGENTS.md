# AGENTS.md — L0 Bootstrap

> Максимально сжатое ядро контекста. Всё остальное грузится по триггерам из `wiki/_routing.md`.
> Этот файл — «конституция» проекта. Агент не редактирует его автономно.

## 1. Кто я

Агент-разработчик проекта **FuckHR**. Отвечаю на русском. Решаю продуктовые задачи
(фичи, баги, рефакторинг), опираясь на контекст этого проекта, а не на общие практики.

TODO: стек, границы ответственности, что делать нельзя без согласования.

## 2. Категории знаний

| Категория | Путь | Что внутри |
| --- | --- | --- |
| Правила | `wiki/rules/` | Проверенные правила с ID-тегами `[TAG-001]` |
| Справочники | `wiki/references/` | Внешние факты: API, лимиты, цены, форматы |
| Архитектура | `wiki/architecture/` | Модули, потоки данных, решения (ADR) |
| Процессы | `wiki/workflows/` | Релизы, ревью, тестирование, инциденты |
| Черновики | `memory/inbox.md` | Свежие наблюдения агента (draft) |

## 3. Триггеры загрузки

1. Задача покрыта этим файлом → работаю сразу.
2. Иначе читаю `wiki/_routing.md` и подгружаю файлы по совпавшим keywords.
3. Если routing не дал результата → `wiki/_index.md` (оглавление, ~200–300 токенов).

## 4. Critical Rules (core)

Нарушение = регрессия. Пополняется только по явному решению владельца проекта.

- `[CORE-001]` Перед добавлением нового правила — `grep` по `wiki/rules/`, чтобы не дублировать знание.
- `[CORE-002]` Неожиданное поведение фиксируется в `memory/inbox.md` сразу, в 3 строки.
- `[CORE-003]` `AGENTS.md` не редактируется автономно; promote `validated → core` требует явного «да».
- TODO: доменные core-правила проекта.

## 5. Цикл самообучения

```
Неожиданность → REGISTER (memory/inbox.md) → PROMOTE (wiki/rules|references) → удаление из inbox
```

Подробности и защитные механизмы: `docs/knowledge-lifecycle.md`.

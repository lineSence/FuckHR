# L3 — Inbox (draft-знания)

> Это не архив уроков, а конвейер. Пишет сам агент, без участия человека.

## Правила

- Лимит: **20 записей**. Записи старше **14 дней** без подтверждения удаляются.
- Формат: ровно 3 строки на запись.
- Триггеры записи:
  1. неожиданное поведение системы;
  2. найденный workaround;
  3. недокументированное поведение API;
  4. явная поправка от пользователя.
- Кандидат на promote: тема встретилась повторно или есть 3+ записи по одной теме.
- После promote запись удаляется отсюда, а в ответе выводится строка:
  `📚 Promoted: [TAG] → wiki/rules/<file>.md`

## Формат

```
- [YYYY-MM-DD] Что ожидалось vs что произошло
  Где: файл / модуль / эндпоинт
  Гипотеза: почему так, что проверить
```

## Записи

- [2026-09-18] Ожидался сервер на 127.0.0.1:8765, а `python webui.py` молча вышел с кодом 0
  Где: webui.py — отсутствовал блок `if __name__ == "__main__"`
  Гипотеза: при разборке webui.py на ui_* модули точка входа потерялась вместе с хвостом файла — проверить остальные запускаемые файлы (outreach.py, maintenance.py) тем же тестом

- [2026-09-18] Ожидались закономерности по отзывам, модель ответила «данных недостаточно, это рекламные заголовки»
  Где: dossier.reviews_from_hits — в анализ шли только title и snippet из websearch.Hit
  Гипотеза: сниппет отзовика — это подпись сайта, а не отзыв; страницы никто не открывал — проверить тот же разрыв в contacts.py, где страницы «о команде» тоже анализируются по выдаче

- [2026-09-18] Ожидался salary_delay на «зарплату задерживают», правило молчало на живом тексте отзыва
  Где: dossier_rules.PATTERN_RULES — игла была только «задерживают зарплат»
  Гипотеза: в русском порядок слов свободный, а иглы писались по одному примеру из теста — проверить на реальных отзывах grey_salary, churn, fake_vacancy, toxic, micromanagement и опечатку «должали зарплату»

- [2026-09-18] Ожидалось, что документация описывает код; оказалось, она отстала на несколько модулей
  Где: docs/architecture.md, wiki/architecture/pipeline.md, tech-stack.md — не было reviewpage.py, параллельных досье, ui_*-разбиения
  Гипотеза: доки правятся только по явному запросу, а не в одном коммите с кодом — перед планированием всегда сверяться с кодом, а не с доками

- [2026-09-18] Ожидалось, что разбиение крупных файлов потребует правки вызовов и тестов — не потребовало
  Где: dossier.py → dossier_rules.py + dossier_store.py; profile_form.py → profile_fields.py; run.py → research.py
  Гипотеза: реэкспорт с `# noqa: F401` держит публичное имя на месте — это рабочий шаблон для [CORE-024]; класть по одному файлу в коммит, иначе пачка больших файлов не проходит за один вызов

- [2026-09-18] Ожидался предпросмотр порога на /profile, блок всегда пустой и молчит
  Где: ui_profile.score_preview — `SELECT ... FROM vacancies WHERE active = 1`, а в db.SCHEMA колонки `active` нет
  Гипотеза: запрос писался под старую схему, а `except sqlite3.Error` глотает ошибку — искать тот же шаблон в остальных ui_* (широкий except вокруг SQL превращает ошибку схемы в пустой экран)

- [2026-09-18] Ожидалось предупреждение про «тимлид», в текст ушло «лид» — тест упал на стороне владельца
  Где: resume.contradictions — перебор GRADE_WORDS через `word in haystack` с break на первом совпадении
  Гипотеза: третий случай одной темы (polarity_of, PATTERN_RULES) — кандидат на promote в wiki/rules: русские иглы искать по границам слова и брать самое длинное/сильное совпадение, а не первое по порядку dict

- [2026-09-19] Ожидался только email из «Пишите на ivan.petrov@romashka.ru», получен ещё и «рабочий Telegram @romashka»
  Где: contacts.TELEGRAM_RE — голая альтернатива @ в регулярке канала
  Гипотеза: тот же класс, что иглы без границ слова: символ-якорь писался под пример «@nickname» и совпал с локальной частью адреса — проверить остальные регулярки каналов (GITHUB_RE, PHONE_RE) на совпадение внутри другого токена

- [2026-09-19] Ожидался руководитель, «руководителем по странице команды» стало любое имя рядом со словом python
  Где: contacts.extract_names — порог role_rank <= 4, а ранг 4 это «python», «backend», «разработчик»
  Гипотеза: косвенный признак («совпало хоть что-то») использовался как факт должности — проверить остальные места, где ранг или счётчик совпадений подменяет собой значение поля: detector.CLAIM_PATTERNS, dossier_rules.PATTERN_RULES

- [2026-09-19] Кейс-ловушка «extract: вилки нет» проваливали все модели владельца (пять разных)
  Где: bench_cases._check_extract — forbidden_fields=("salary",) обнуляло сам факт поля salary, хотя про зарплату в тексте сказано и цитата дословная
  Гипотеза: запрет поля использовался вместо запрета выдуманного значения — проверить остальные места, где проверка стоит на имени поля, а не на его содержимом: detector.CLAIM_PATTERNS, dossier_rules

- [2026-09-19] Сводка по отзывам уходила этапом «company», хотя профиль этапа dossier — LOCAL и он в PERSONAL_STAGES
  Где: dossier.summarize — в gateway.complete стояло имя чужого этапа, маршрут ПД выбирался по профилю LONG
  Гипотеза: имя этапа писалось строкой по месту вызова — проверить остальные complete(): совпадает ли имя этапа с тем, чей профиль имелся в виду

- [2026-09-19] Запросы поиска контактов не зависели от вакансии: на «Оператора 1С» уходили «тимлид backend» и «инженерный блог habr»
  Где: websearch.contact_queries — параметр roles был, но ни один вызов его не передавал, работал дефолт
  Гипотеза: параметр добавили «на будущее» и забыли прокинуть — проверить остальные необязательные параметры этапов на такие же мёртвые умолчания

- [2026-09-19] Сбор с лимитом 1000 приносил 400–500: упирались не в лимит, а в потолок страниц
  Где: hh_html.search/hh.search max_pages=3..5 по умолчанию плюс profile_schema.Query.max_pages=3 — лимит прогона был недостижим
  Гипотеза: умолчание писалось под ручную отладку и осталось в проде — проверить остальные «безопасные» умолчания, которые молча режут объём: REVIEW_FETCH_PAGES, SEARCH_PAGES

- [2026-09-19] Ожидалось, что даты отзывов есть на страницах, по факту на части площадок они относительные («3 дня назад») или отсутствуют
  Где: reviewitems.parse_date, сигнал burst в fake_reviews
  Гипотеза: окно всплеска в 7 дней считается только по датам с известным днём — проверить на живом дампе (REVIEW_DUMP_PATH), какая доля отзывов остаётся без дня

- [2026-09-19] Рынок считается по нашей же выдаче: смещение выборки профилем поиска не измеряется ничем
  Где: market_store.recompute — срезы строятся из market_observations, а те снимаются только с тех запросов, что есть в profile.yaml
  Гипотеза: «рынок» подменяется «тем, что мы искали» — проверить на живых данных долю наблюдений с неопознанной ролью (market.role_of вернул "") и долю вилок «до X»: если первая велика, словарь ROLE_FAMILIES отстаёт, если вторая — медиана завышена рекламой

- [2026-09-19] Детектор сгенерированного текста включён с непроверенными порогами: доля ложных срабатываний неизвестна
  Где: aitext_rules.SUSPECT_AT и веса SIGNALS — числа из проектного документа, размеченной выборки не было
  Гипотеза: «шаблонный текст» и «текст модели» — разные явления, и первое встречается у живых кадровиков сплошь — проверить на текстах до 2022 года, сколько человеческих описаний получают метку: если больше 5%, поднимать порог, а не добавлять признаки

- [2026-09-19] Разбор страниц отзовиков ни разу не проверялся на живой вёрстке: все тесты на синтетическом HTML, который написан тем же, кто писал разбор
  Где: reviewitems.split_page, reviewpage.extract_reviews — блок ищется по слову review в классе, строка по словарю подсказок
  Гипотеза: правила подогнаны под воображаемую вёрстку — собрать образцы (REVIEW_PAGE_DUMP_DIR) и посмотреть на реальных страницах: сколько отзывов выходит, какая доля без даты и не режется ли один отзыв на два

- [2026-09-19] Разбор страниц отзовиков на живой вёрстке терял всё: 0 дат, 0 оценок, один отзыв резался на три куска, с одной площадки не выходило ни одного отзыва
  Где: reviewitems.BLOCK_RE — блок искался по подстроке «review» в классе, под это подходили review__text, review__header и 837 ссылок review-other-city-item
  Гипотеза: правило писалось под воображаемую вёрстку и проверялось синтетикой от того же автора — проверить тем же способом остальные разборы, которые тестируются только своими же примерами: hh_html (карточка вакансии), contacts.extract_names, websearch (выдача провайдеров)

- [2026-09-20] Ожидался зелёный pytest на чистой ветке, два теста падают и без наших правок (528 passed, 2 failed)
  Где: tests/test_hh_pages.py::test_известная_страница_останавливает_обход и tests/test_settings_catalog.py (MAP_TILE_URL, GEO_BACKFILL_LIMIT читаются кодом, но их нет в каталоге)
  Гипотеза: каталог настроек пополняется вручную и отстаёт от кода; в лоб не чинится — settings_fields.py уже ≈ 25 КБ [CORE-024], сначала делить файл, потом добавлять ключи

- [2026-09-20] Ожидалось, что соседние таблицы создаёт слой данных, а ensure_schema вызывался только из ui_views
  Где: filters.ensure_tables — собраны четыре вызова (contacts, detector, injection_store, company_score_store), которые жили в слое интерфейса
  Гипотеза: любой новый потребитель фильтров (карта) получал пустой экран или ошибку на чистой базе — проверить остальные ensure_schema, вызываемые только из ui_*

- [2026-09-21] Ожидалось, что `bench.py --route local` меряет названную модель, а в Ollama уходило имя профиля (`auto:fast`) — «model not found» и одинаковый ноль у всех локальных моделей
  Где: bench.gateway_for — на локальном маршруте model нигде не сохранялся, local_models оставался пустым, хотя LLM_LOCAL_MODEL_* появились ещё в шлюзе
  Гипотеза: имена локальных моделей добавили в Gateway, а второго потребителя (бенч) не обновили — проверить остальные места, где Gateway собирается руками, а не from_env: llm_embed, check_llm, тесты

- [2026-09-21] Ожидалось, что сохранённой разметки отзывов хватит на примеры для дообучения: в review_items лежит только выдержка в 240 символов, а вердикт модель выносила по 1200
  Где: fake_store.EXCERPT_CHARS против fake_llm.MAX_CHARS — этапы review_fake и ai_text пришлось исключить из dataset_export
  Гипотеза: выдержка задумана для показа в интерфейсе, а не для воспроизведения решения — проверить, где ещё храним обрезку вместо входа этапа: dossier (summary без текстов, что видела модель), injection_store, contact_finds

- [2026-09-21] Ожидалось, что при обычном прогоне адреса пишутся в базу (это и обещано в docs/map.md: «Новые вакансии получают адрес сразу при сборе»), а geo.from_page вызывался только из geo_backfill — карта наполнялась лишь кнопкой
  Где: run.py — client.vacancy() уже разбирал состояние страницы ради описания, но адрес из него никто не брал; geo.save при этом возвращал True, даже когда UPDATE не нашёл строки
  Гипотеза: карту делали после сбора и подключили только дозаполнение, а документацию писали по замыслу — проверить остальные обещания docs/ на живом коде, начиная с тех, где есть кнопка «собрать всё» (адреса, датасет, оценка работодателя)

- [2026-09-21] Ожидалось, что очистка базы обнуляет таблицу «Площадки в сборе» на главной, а она показывала цифры удалённого сбора: vacancy_sources не входила в цель «Вакансии»
  Где: maintenance.TARGETS + ui_sources.render_sources — счётчики считались по разметке, которую никто не чистил; source_store.drop_orphans существовал, но не вызывался
  Гипотеза: таблицу добавили ради метрики и забыли про необратимую половину системы — проверить остальные молодые таблицы на присутствие в maintenance.TARGETS: geo-точки, injection_hits, embeddings_tasks

- [2026-09-22] Ожидалось, что при снятой галочке hh.ru прогон туда не ходит, а run.py звал profiles.collect_all и targets_hh.sweep всегда: владелец выбрал только Работу.ру и получил 43 вакансии с hh
  Где: run.py — sources.selected() читал только collect_external, hh собирался отдельным путём выше и про выбор площадок не знал
  Гипотеза: мультиисточник пристроили рядом с hh, а не вокруг него — проверить остальные места, где hh зашит как «всегда»: hh_pages.search_cache, targets_hh, geo, hh_employer

- [2026-09-22] Ожидалось, что вакансия с Работы.ру не ходит на hh.ru за деталями, а run.py открывал hh.ru/vacancy/<id чужой площадки> — id восьмизначный и на hh существует, поэтому приезжали чужое описание и чужой адрес, и карта показывала точки по всей России без единой ошибки в логах
  Где: hh_pages.worth_details и geo.pending не смотрели на Vacancy.source; enrich() подменял описание молча
  Гипотеза: до мультиисточника external_id был однозначным, и код это допущение нигде не проверял — проверить всех, кто строит адрес из external_id или разбирает /vacancy/(\d+): geo.vacancy_id, hh_employer, targets_hh, bot

- [2026-09-22] Ожидалось, что фильтр региона у Работы.ру работает параметром city, а площадка его игнорирует: регион задаётся поддоменом, а www.rabota.ru — это Москва, не Россия
  Где: src_rabota.search + src_common.AREA_MAP — код региона переводился в значение параметра, которого у площадки нет
  Гипотеза: адаптер писался по аналогии с API-площадками, а фильтр на живой выдаче не сверялся — проверить так же Zarplata.ru: ей уходят коды area от hh.ru, но справочник у неё может быть свой

- [2026-09-22] Ожидалось, что Zarplata.ru просто не отвечает, а она отказывалась от нашего Accept: строка с application/json давала 406 Not Acceptable, браузерная — 200 и полную выдачу
  Где: src_common.HEADERS — свой Accept вместо браузерного; Fetcher.text логировал «ответил 406» одной строкой warning и шёл дальше [CORE-017]
  Гипотеза: деградация вместо остановки прячет причину — стоит отличать в логе «не ответил» от «отказался от запроса» (4xx на наши заголовки), иначе площадка месяцами числится живой и пустой

- [2026-09-22] Ожидалось, что в логе прогона видно ход сбора, а его занимали чужие INFO: httpx писал строку на каждый запрос, huggingface_hub — семь запросов к huggingface.co при каждом старте GLiNER, плюс FutureWarning от torch.jit
  Где: run_setup.setup_logging ставил уровень всем логгерам разом; extract_spans.load звал GLiNER.from_pretrained без HF_HUB_OFFLINE, хотя веса уже в кэше
  Гипотеза: свои логгеры настраивали, чужие не считали своей заботой — стоит держать список болтливых в одном месте (run_setup.NOISY) и звать его из каждой точки входа, а не по одной чинить

- [2026-09-22] Ожидалось, что поиск по вакансиям и компаниям не смотрит на регистр, а по-русски он был регистрозависимым: «оператор» не находил «Оператор 1С», хотя «operator» находил «Operator»
  Где: filters.like + LIKE в SQL — встроенные LIKE, lower() и COLLATE NOCASE в SQLite приводят регистр только для латиницы (сборка без ICU); теперь filters.register регистрирует ru_lower, условия собирает filters.ilike
  Гипотеза: проверяли на латинице и на коротких примерах — стоит проверить остальные места, где сравниваем текст из базы: company_key, market-разрезы по должностям, поиск по контактам, дедуп отзывов
